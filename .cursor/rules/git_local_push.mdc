---
description: Local Git commit/push on Windows — release-branch merges for Amplify, untracked files, and secret filtering before stage
globs: **/*
alwaysApply: false
---

- **Scope**
  - Applies when committing code changes from the local workspace using Git CLI on Windows PowerShell.
  - Use for routine commits and pushes to existing or new branches.
  - **Also use** when pushing frontend repos so **AWS Amplify production builds** trigger on the correct branch.
  - When the user asks to commit/push the **core trio** (mosc-temp + batch-jobs + service together), also follow [git_commit_push_core_repos.mdc](mdc:.cursor/rules/git_commit_push_core_repos.mdc).
  - **One-shot prompt** (commit + unstaged files + release deploy): [git_commit_push_release_deploy.mdc](mdc:.cursor/rules/git_commit_push_release_deploy.mdc).
  - **Deploy to Amplify/ECS** (after commit, push release + verify builds): [deploy_apps_to_amplify_ecs.mdc](mdc:.cursor/rules/deploy_apps_to_amplify_ecs.mdc).

- **Pre-checks**
  - **GitHub auth (load before every push on this machine)** — see [deploy_apps_to_amplify_ecs.mdc](mdc:.cursor/rules/deploy_apps_to_amplify_ecs.mdc) Phase 0:
    ```powershell
    $env:GH_TOKEN = [Environment]::GetEnvironmentVariable('GH_TOKEN', 'User')
    gh auth setup-git
    $env:GIT_SSL_NO_VERIFY = 'true'
    gh auth status   # expect: Logged in … (GH_TOKEN)
    ```
    - **DO NOT** run interactive `gh auth login` while `GH_TOKEN` is set and valid.
    - If push fails with invalid default token but `GH_TOKEN` works, load User token again and retry.
  - **Verify current branch**:
    ```powershell
    git status -sb
    git branch -a | Select-String "release"
    ```
  - If you need a new branch:
    ```powershell
    git checkout -b <new-branch-name>
    ```

---

## **Phase 0 — Pull remote FIRST (before commit or release merge)**

**Always `fetch` + `pull` the current work branch** before staging, committing, or merging to release. Do **not** commit on a branch that is behind `origin` without pulling first.

```powershell
cd F:\project_workspace\<repo>
$branch = git branch --show-current
$env:GIT_SSL_NO_VERIFY = 'true'
git fetch origin
git status -sb
git log HEAD..origin/$branch --oneline
git pull origin $branch
Remove-Item Env:GIT_SSL_NO_VERIFY -ErrorAction SilentlyContinue
```

| `git pull` result | Action |
|-------------------|--------|
| **Already up to date** | Proceed to Phase 1 (stage/commit) |
| **Fast-forward** | Remote changes applied — proceed |
| **Merge commit created** | Pull succeeded — proceed |
| **CONFLICT** | **STOP** — resolve conflicts (below) before commit/push |
| **error: Your local changes would be overwritten** | Stash or commit WIP first, then pull again |

**Detect unresolved conflicts:**

```powershell
git diff --name-only --diff-filter=U
git diff --check
Select-String -Path <file> -Pattern '^(<<<<<<<|=======|>>>>>>>)' 
```

**Resolve conflicts (agent must attempt before reporting blocker):**

1. Open each file listed by `--diff-filter=U`.
2. Choose correct hunks (or combine); **remove all** `<<<<<<<`, `=======`, `>>>>>>>` markers.
3. Run `git add <file>` for each resolved file.
4. Complete the merge:
   ```powershell
   git commit -m "merge: resolve conflicts after pull from origin/<branch>"
   ```
   (Or `git merge --continue` / `git rebase --continue` if a rebase was in progress.)

**If conflicts cannot be resolved safely** (ambiguous business logic, same file edited unrelated ways, binary conflicts):

- **DO NOT** commit or push that repo.
- **Report as BLOCKER** in the response (see **Blocker report format** below).
- Continue with other requested repos that have no blockers.

---

## **Amplify deploy: merge to release branch before push**

When the goal is to **trigger AWS Amplify** (production / satellite frontends), the push must land on the branch Amplify watches — not only the feature branch.

| Repo | Typical path | Amplify production branch | Action if not on that branch |
|------|--------------|----------------------------|------------------------------|
| `mosc-temp` | `F:\project_workspace\mosc-temp` | `release/v1.0.0` | Commit → merge/fast-forward into `release/v1.0.0` → push `release/v1.0.0` |
| `event-site-front-end-test-site` | `F:\project_workspace\event-site-front-end-test-site` | `release/v1.0.0` | Commit on `release/v1.0.0` → push |
| `event-site-front-end-template` | `F:\project_workspace\event-site-front-end-template` | `main` (no `release/*` on remote) | Commit on `main` → push `main` |
| `strapi-editorial-template` | `F:\project_workspace\strapi-editorial-template` | `release/v1.0.0` or `main` | Commit on `main` → FF/merge `release/v1.0.0` if prod uses it |
| `event-site-manager-batch-jobs` | `F:\project_workspace\event-site-manager-batch-jobs` | `release/v1.0.0` | Commit on `main` → merge into `release/v1.0.0` → push |
| `event-site-manager-service` | `F:\project_workspace\event-site-manager-service` | `release/v1.0.0` | Commit on feature branch → merge into `release/v1.0.0` → push |
| `event-site-manager` (primary) | `F:\project_workspace\event-site-manager` | Check Amplify console / docs | Usually `release/v1.0.0` or `main` per app |

**Workflow (feature branch → release) — pull, merge, resolve, then push or PR:**

```powershell
cd F:\project_workspace\<repo>
$work = git branch --show-current   # or explicit: MOSC-redesign-sub-poc-2, main, etc.

# 0) Pull work branch first (Phase 0)
git fetch origin
git pull origin $work

# 1) Commit on work branch (after secret scan below)
git add -A
# ... secret filter ...
git commit -m "feat(scope): short imperative message"
git push origin $work

# 2) Preview release merge LOCALLY — resolve conflicts before push
git fetch origin
git checkout release/v1.0.0
git pull origin release/v1.0.0
git merge $work -m "merge: $work into release/v1.0.0 for deploy"

# If conflicts: resolve files, git add, git commit (merge --continue)
# If CRLF-only drift and no real conflicts: git reset --hard $work

# 3) Push release OR open PR if protected
$env:GIT_SSL_NO_VERIFY = "true"
git push origin release/v1.0.0
Remove-Item Env:GIT_SSL_NO_VERIFY
git checkout $work
```

**If `git push origin release/v1.0.0` is rejected** (protected branch / PR required):

```powershell
# Work branch must already be pushed
gh pr create --base release/v1.0.0 --head $work `
  --title "merge: $work into release/v1.0.0 for deploy" `
  --body "Automated release sync. Local merge conflicts resolved: <yes/no>."

# Check PR mergeability; merge if clean
gh pr view --json mergeable,mergeStateStatus,url
gh pr merge <number> --merge --delete-branch=false
```

- **DO:** Resolve merge conflicts **locally** on `release/v1.0.0` (or on work branch during pull) **before** push or PR.
- **DO:** Open a **pull request** when direct push to `release/v1.0.0` is blocked; attempt `gh pr merge` after conflicts are resolved.
- **DON'T:** Push only the work branch and assume prod deployed.
- **DON'T:** Force-push `release/v1.0.0` without explicit user approval.

---

## **Unstaged (` M`) and untracked (`??`) files — include in every commit**

When the user asks to **commit and push** a project and `git status` shows **any** unstaged or untracked paths, stage **all safe changes** — not only the paths they named (e.g. not only `.cursor/rules/*` while app code stays dirty).

**Mandatory condition:** If `git status --porcelain` shows **` M` (modified, unstaged)**, **`MM` (staged + unstaged)**, or **`??` (untracked)**, run **`git add -A`** and commit every path that passes the secret filter below. Unstaged tracked files **must not** be left behind when the user requests commit/push — they are first-class commit candidates, same as untracked files.

```powershell
git status -sb
git status --porcelain
# If any line starts with " M", "MM", "??", " D", " A", etc. — stage all safe paths:
git add -A
git diff --cached --name-only
```

**Required workflow:**

1. **List pending work** with `git status --porcelain` before staging.
2. **`git add -A`** — includes modified tracked files **and** new untracked files.
3. **Unstage excluded paths** (build artifacts + secret blocklist):
   ```powershell
   git restore --staged tsconfig.tsbuildinfo 2>$null
   git restore --staged code_html_template/SQLS/corrected_event_media_inserts.ordered_PROD.sql 2>$null
   git diff --cached --name-only | Select-String -Pattern `
     'prod\.env$|\.env\.local|\.env\.production|auth\.json$|\.auth-state|secrets|\.pem$|\.key$|credentials|ordered_PROD\.sql|tsconfig\.tsbuildinfo$' `
     -CaseSensitive:$false | ForEach-Object { git restore --staged $_.Line.Trim() }
   ```
4. **Secret filename check** (below) — empty output required before commit.
5. **Content spot-check** on new untracked env-like or credential-shaped files (below).
6. **`git commit`** with a message reflecting the **full** staged diff.
7. **Verify clean tree** after push:
   ```powershell
   git status -sb
   ```
   If commit-eligible paths remain dirty (including **` M` unstaged** lines in porcelain), repeat from step 1. Only `tsconfig.tsbuildinfo`, gitignored env files, runtime logs (e.g. `*.log` with OAuth output), and other blocklisted paths may stay unstaged.

**DO:** Include **unstaged modified** tracked files (` M`), new feature folders (`?? src/**`), docs, SQL refresh artifacts, tenant settings, and modified tracked files in the same commit when they pass the secret filter.

**DON'T:** Leave unstaged app/docs/script changes uncommitted after a commit/push request — if porcelain still shows ` M` or `??` on safe paths, the workflow is incomplete.

**DON'T:** Commit only `.cursor/rules/*` while leaving other unstaged/untracked app changes behind — unless the user **explicitly** scoped the request to rules only.

---

## **Include untracked (unversioned) files — with categorization**

When the user asks to **add unversioned files**, stage everything **after** secret filtering — not only modified tracked files (same as the unstaged/untracked workflow above).

```powershell
git status -sb
git add -A
git diff --cached --name-only
```

**Categorize staged paths before commit:**

| Category | Examples | Usually commit? |
|----------|----------|-----------------|
| App / feature code | `src/**`, `components/**` | ✅ Yes |
| Backend Java (Spring) | `src/main/java/**`, `src/test/java/**`, `src/main/resources/**` | ✅ Yes — include when porcelain shows ` M` |
| Cursor rules | `.cursor/rules/*.mdc` | ✅ Yes |
| Docs / runbooks | `documentation/**`, `*.html` PRDs | ✅ Yes |
| Migration scripts | `scripts/mosc-in-migration/**` | ✅ Yes (env vars for DB, not hardcoded prod secrets) |
| Manifest / map JSON | `url-list.*.json`, `*-category-map.json` | ✅ Yes (metadata/URLs) |
| SQL refresh artifacts | `export.sql`, `*.ordered.sql`, `sequence_fix_inserts/**` | ✅ Yes (unless gitignored) |
| Env **templates** | `*.env.example`, `database_target.*.env.example` | ✅ Yes (placeholders only) |
| Local caches | `.mosc-downloads/`, `**/screenshots/` | ❌ No — should be gitignored |
| Build artifacts | `tsconfig.tsbuildinfo`, `.next/` | ❌ No — gitignore or skip |
| **Secrets / credentials** | See blocklist below | ❌ **Never** |

---

## **Secret and key filtering (required before `git commit`)**

**Always run a staged-path check** after `git add -A` and **before** `git commit`:

```powershell
git diff --cached --name-only | Select-String -Pattern `
  'prod\.env$|\.env\.local|\.env\.production|auth\.json|\.auth-state|secrets|\.pem$|\.key$|credentials' `
  -CaseSensitive:$false
```

- **Empty output** = no blocked filenames in the index (good).
- **Any match** = unstage and gitignore before committing:
  ```powershell
  git reset HEAD -- <path>
  ```

**Filename blocklist (never commit):**

| Pattern / file | Why |
|----------------|-----|
| `code_html_template/batch_files/database_target.prod.env` | Production RDS password |
| `code_html_template/batch_files/database_target.local.env` | Local/dev RDS credentials |
| `.env`, `.env.local`, `.env.production`, `.env.*` (except `.env.example`) | App secrets, Clerk, Stripe, JWT |
| `TestSprite/**/auth.json`, `**/.auth-state.json` | Session tokens |
| `**/secrets-snapshot.json`, `**/*credentials*` | AWS / API secrets |
| `*.pem`, `*.key`, `*.p12` | Private keys |
| `code_html_template/SQLS/corrected_event_media_inserts.ordered_PROD.sql` | May contain encrypted payment/Twilio blobs (gitignored in mosc-temp) |

**Ensure `.gitignore` includes (add if missing):**

```gitignore
.env*
!.env.example
code_html_template/batch_files/database_target.local.env
code_html_template/batch_files/database_target.prod.env
code_html_template/SQLS/corrected_event_media_inserts.ordered_PROD.sql
```

**Git LFS for SQL exports >100 MB (GitHub hard limit):**

When `export.sql`, `corrected_event_media_inserts.ordered.sql`, or `sequence_fix_inserts/corrected_event_media_inserts.renumbered.sql` exceed **100 MB**, GitHub rejects the push. Track them with Git LFS **before** commit:

```powershell
git lfs install
git lfs track "code_html_template/SQLS/export.sql"
git lfs track "code_html_template/SQLS/corrected_event_media_inserts.ordered.sql"
git lfs track "code_html_template/SQLS/sequence_fix_inserts/corrected_event_media_inserts.renumbered.sql"
git add .gitattributes
git add -A
# ... secret filter ...
git commit -m "chore(db): refresh export SQL (Git LFS)"
git push origin <branch>
```

Reference: `mosc-temp/.gitattributes`. **`auth.json.example`** is safe to commit; blocklist uses `auth.json$` (exact filename) so examples are not excluded.

**Content spot-check** for new untracked env-like files:

```powershell
Get-Content path\to\suspect-file | Select-Object -First 20
```

Reject if you see real `DB_PASSWORD=`, `sk_live_`, `sk_test_`, `AKIA…`, `Bearer …`, or `BEGIN RSA PRIVATE KEY`.

**Migration scripts:** OK to commit when they use `process.env.MOSC_DB_PASSWORD` / `process.env.DB_PASSWORD` — not hardcoded production passwords.

---

## **Stage → Commit → Push (Windows PowerShell)**

- PowerShell does not support `&&` as a command separator by default. Run each command on its own line.

**Recommended sequence (pull first, then stage, commit, push):**

```powershell
cd F:\project_workspace\<repo>
$branch = git branch --show-current
git status -sb

# Phase 0: pull remote first
$env:GIT_SSL_NO_VERIFY = "true"
git fetch origin
git pull origin $branch
Remove-Item Env:GIT_SSL_NO_VERIFY -ErrorAction SilentlyContinue

# Stop if conflicts — resolve before continuing
git diff --name-only --diff-filter=U

# Optional: update .gitignore for prod/local env files first
git add .gitignore

git add -A
git diff --cached --name-only | Select-String -Pattern 'prod\.env|\.env\.local|auth\.json|secrets' -CaseSensitive:$false
# Fix any matches, then:
git commit -m "feat(scope): short imperative message"

$env:GH_TOKEN = [Environment]::GetEnvironmentVariable('GH_TOKEN', 'User')
gh auth setup-git
$env:GIT_SSL_NO_VERIFY = "true"
git push origin $branch
Remove-Item Env:GIT_SSL_NO_VERIFY
```

- **Stage specific paths** (when not using `-A`):
  ```powershell
  git add <path1> <path2> <pathN>
  ```
- **Commit with message**:
  ```powershell
  git commit -m "<type(scope): short, imperative message>"
  ```
- **Push to upstream (existing tracking branch)**:
  ```powershell
  git push
  ```
- **First push for a new branch (set upstream tracking)**:
  ```powershell
  git push -u origin <branch-name>
  ```

---

## **Examples**

- **Satellite frontend (mosc-temp): commit feature + deploy via release:**
  ```powershell
  cd F:\project_workspace\mosc-temp
  git add -A
  git diff --cached --name-only | Select-String 'prod\.env|auth\.json'
  git commit -m "feat(mosc-redesign): downloads migration and SQL refresh"
  git checkout release/v1.0.0
  git reset --hard MOSC-redesign-sub-poc-2
  $env:GIT_SSL_NO_VERIFY = "true"
  git push origin release/v1.0.0
  Remove-Item Env:GIT_SSL_NO_VERIFY
  ```

- **Test-site (already on release):**
  ```powershell
  cd F:\project_workspace\event-site-front-end-test-site
  git add -A
  git diff --cached --name-only | Select-String 'prod\.env|auth\.json'
  git commit -m "feat(mosc-redesign): sync with mosc-temp baseline"
  $env:GIT_SSL_NO_VERIFY = "true"
  git push origin release/v1.0.0
  Remove-Item Env:GIT_SSL_NO_VERIFY
  ```

- **Template (main only):**
  ```powershell
  cd F:\project_workspace\event-site-front-end-template
  git add -A
  git commit -m "feat: sync cursor rules and migration tooling"
  git push origin main
  ```

- **Create and push a new branch:**
  ```powershell
  git checkout -b payment-options
  git push -u origin payment-options
  ```

---

## **Authentication prompts**

- If Git asks to authenticate, a browser window may open. Complete auth and re-run `git push` if needed.

---

## **Troubleshooting**

- If you see `The token '&&' is not a valid statement separator`, split combined commands into separate lines.
- Normalize line endings warning (LF→CRLF) is informational. Commit proceeds unless you configured otherwise.
- **`git pull` conflicts:** resolve markers, `git add`, `git commit`; if unresolvable → **BLOCKER** (do not push).
- **Merge blocked on release with “local changes would be overwritten”** (CRLF drift): stash or commit WIP, then `git checkout release/v1.0.0`, `git pull`, `git merge <work-branch>`; if only CRLF drift, `git reset --hard <work-branch>` then push.
- **Protected `release/v1.0.0` push rejected:** create PR with `gh pr create`, resolve merge conflicts on PR or locally first, then `gh pr merge`.
- **`database_target.prod.env` shows as untracked (`??`)**: add to `.gitignore`; never `git add` it.

---

## **Blocker report format (required in agent response)**

When any requested repo cannot complete pull, merge, commit, or push, report a **BLOCKERS** section:

```markdown
### BLOCKERS
| Repo | Step | Issue | Conflicted files | Action needed |
|------|------|-------|------------------|---------------|
| mosc-temp | pull / release merge | Unresolved conflicts | `src/foo.tsx`, `package.json` | Manual merge decision |
| batch-jobs | push release | PR required, merge conflicts on PR | — | Resolve PR #N |

### COMPLETED (no blockers)
| Repo | Work branch | Commit | Release/PR | Status |
|------|-------------|--------|------------|--------|
| test-site | main | abc1234 | release/v1.0.0 @ abc1234 | pushed |
```

- **DO:** List every conflicted file path for unresolved merges.
- **DO:** Continue commit/push for repos with **no** blockers in the same request.
- **DON'T:** Skip reporting a failed repo silently.

---

## **SSL certificate errors on `git push` / `git fetch` (Windows)**

- **Symptom** (seen repeatedly on `mosc-temp` and other HTTPS remotes):
  ```
  fatal: unable to access 'https://github.com/...': SSL certificate problem: unable to get local issuer certificate
  ```
- **Cause:** Git for Windows cannot validate the HTTPS server certificate chain (missing/intermediate CA, corporate SSL inspection, or outdated CA bundle).
- **Temporary workaround (current session only — use only to unblock a push):**
  ```powershell
  cd F:\project_workspace\<repo>
  $env:GIT_SSL_NO_VERIFY = "true"
  git push
  Remove-Item Env:GIT_SSL_NO_VERIFY
  ```
  - **Do not** set `git config http.sslVerify false` globally.
  - **Do not** leave `GIT_SSL_NO_VERIFY` set in your user or system environment.
- **Proper fix (preferred — pick one):**
  1. **Update Git for Windows** — [https://git-scm.com/download/win](https://git-scm.com/download/win)
  2. **Corporate proxy / SSL inspection** — `git config --global http.sslCAInfo "C:/path/to/corporate-ca-bundle.pem"`
  3. **Switch the remote to SSH** — `git remote set-url origin git@github.com:org/repo.git`
- **Verify after fix:** `git ls-remote origin HEAD`

---

## **Best practices**

- **Always `git pull` before `git commit`** on the work branch (Phase 0).
- **Resolve merge conflicts locally** before push or PR; report unresolvable conflicts as blockers.
- Use conventional commit messages when possible (e.g., `feat`, `fix`, `chore`).
- Keep commits focused and scoped; avoid bundling unrelated changes.
- **Always** run the staged secret filename check before commit when using `git add -A`.
- For Amplify prod deploys, push the **documented production branch** (`release/v1.0.0` or `main`) — use **PR** when direct push is blocked.
- Push to a feature branch first when using PR-based release protection; merge via PR if direct push to release is blocked.

---

## **References**

- **New project — first-time GitHub setup:** [git_new_project_setup.mdc](mdc:.cursor/rules/git_new_project_setup.mdc)
- **One-shot commit + push + release deploy:** [git_commit_push_release_deploy.mdc](mdc:.cursor/rules/git_commit_push_release_deploy.mdc)
- **Core trio batch commit/push** (mosc-temp, batch-jobs, service): [git_commit_push_core_repos.mdc](mdc:.cursor/rules/git_commit_push_core_repos.mdc)
- Amplify / satellite branches: [satellite-frontend-deployment-guide.html](mdc:documentation/aws-infrastructure/satellite-frontend-deployment-guide.html) (see `application-deployments` repo for this doc)
- Database import (separate from git): [database_export_import_vpc_local.mdc](mdc:.cursor/rules/database_export_import_vpc_local.mdc) (see `application-deployments` repo)
- Rule format: [cursor_rules.mdc](mdc:.cursor/rules/cursor_rules.mdc)
