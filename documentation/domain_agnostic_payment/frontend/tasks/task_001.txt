# Task ID: 1
# Title: Set up API proxy endpoints structure
# Status: pending
# Dependencies: None
# Priority: high
# Description: Create the necessary proxy endpoint structure following the required patterns for REST API calls.
# Details:
- Create proxy routes under `src/pages/api/proxy`
- Implement proxy pattern according to `cursor_rules/nextjs_api_routes.mdc`
- Set up `/api/proxy/payments/initialize` endpoint
- Set up `/api/proxy/payments/{transactionId}` endpoint for polling
- Set up `/api/proxy/memberships/subscriptions` endpoint

# Test Strategy:
Verify that all proxy endpoints correctly forward requests to the backend and handle responses appropriately.
